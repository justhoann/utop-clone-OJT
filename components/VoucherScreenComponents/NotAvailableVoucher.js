import { StyleSheet, Text, View } from "react-native";
import React from "react";

export default function NotAvailableVoucher() {
	return (
		<View>
			<Text>NotAvailableVoucher</Text>
		</View>
	);
}

const styles = StyleSheet.create({});
